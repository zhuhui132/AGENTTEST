# 🤝 贡献指南

感谢你愿意参与改进 AgentTest！以下流程适用于代码、文档与示例的任何改动。

---

## 1. 快速开始

```bash
git clone https://github.com/<your-username>/agentTest.git
cd agentTest
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install -r requirements-dev.txt  # 需要运行测试时

# 新建工作分支
git checkout -b feature/<topic>
```

---

## 2. 提交前检查清单

- [ ] 代码通过自测（至少运行 `examples/quickstart.py`）
- [ ] 如涉及核心逻辑，补充或更新对应单元测试
- [ ] 文档与注释同步更新（若影响到外部接口）
- [ ] `requirements*.txt` 中新增的依赖已注明用途
- [ ] `git status` 保持干净，只提交相关文件

---

## 3. Pull Request 说明模板

```markdown
## 变更内容
- 做了什么，修改了哪些模块

## 测试
- [ ] pytest
- [ ] 手动验证：...

## 其他
- 风险评估 / 回滚方案（如有）
```

> PR 标题建议使用 `type(scope): summary`，例如 `feat(memory): add redis backend`。

---

## 4. 沟通渠道

- Bug、功能建议：GitHub Issues
- 讨论、提问：GitHub Discussions
- 紧急问题：邮件至 maintainer@example.com

---

## 5. 许可证声明

提交 PR 即表示你同意以项目采用的 MIT 许可发布你的贡献，并保证拥有相关代码/文档的著作权或转授权。

再次感谢你的贡献！
